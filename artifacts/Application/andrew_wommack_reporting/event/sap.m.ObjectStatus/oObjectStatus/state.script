if (typeof partner === "undefined" || partner === null || partner === "") { return; }
var formattedText = partner;

if (partner === "Yes") {
    var formattedText = "Success";
}
else {
    var formattedText = "Information";
}
return formattedText;